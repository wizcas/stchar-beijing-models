# copy-status 脚本使用指南

## 功能

将 `status.json` 压缩成单行 JSON 并自动复制到剪贴板。

## 使用方法

### 方式 1: npm 脚本（推荐）

```bash
npm run copy:status
```

### 方式 2: 直接运行

```bash
node copy-status.js
```

## 功能特性

✅ **自动压缩** - 移除 JSON 中的所有空白字符（空格、换行等）
✅ **自动复制** - 自动复制到系统剪贴板
✅ **跨平台支持** - 支持 macOS、Linux、Windows
✅ **压缩统计** - 显示原始大小、压缩后大小和压缩率
✅ **容错处理** - 剪贴板工具不可用时输出到标准输出

## 输出示例

```
📊 压缩统计:
   原始大小: 2223 字符
   压缩大小: 1460 字符
   压缩率: 34.3%

✅ status.json 已压缩并复制到剪贴板 (macOS)
```

## Linux 依赖

在 Linux 上需要以下工具之一：

- **xclip** (Debian/Ubuntu): `sudo apt install xclip`
- **xsel** (Debian/Ubuntu): `sudo apt install xsel`  
- **wl-copy** (Wayland): 通常预装在 Fedora 等发行版

如果都没有安装，脚本会输出压缩内容到标准输出，可手动复制。

## 使用场景

1. **快速分享数据** - 将完整状态压缩成一行便于分享
2. **粘贴到其他系统** - 一行 JSON 便于在各种环境中使用
3. **数据备份** - 快速获取当前状态的压缩版本
4. **调试** - 生成最小化的 JSON 便于排查问题

## 技巧

### 保存到文件

```bash
node copy-status.js > status-compressed.json
```

### 使用管道

```bash
node copy-status.js | xclip -selection clipboard
```

### 在脚本中使用

```bash
#!/bin/bash
npm run copy:status
echo "Status data copied to clipboard!"
```
